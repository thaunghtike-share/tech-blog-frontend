apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  annotations:
    certmanager.k8s.io/acme-challenge-type: dns01
    certmanager.k8s.io/acme-dns01-provider: cloudflare
  labels:
    app: ldn-ui
  name: ldn-mm-mobile-ingress
  namespace: learndevopsnow
spec:
  ingressClassName: nginx
  rules:
    - host: mobile.learndevopsnow-mm.blog
      http:
        paths:
          - backend:
              service:
                name: ldn-ui-v3-mobile
                port:
                  number: 80
            path: /
            pathType: Prefix
  tls:
    - hosts:
        - mobile.learndevopsnow-mm.blog
      secretName: tls-secret-ldn-mm