apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  annotations:
    certmanager.k8s.io/acme-challenge-type: dns01
    certmanager.k8s.io/acme-dns01-provider: cloudflare
  labels:
    app: ldn-ui
  name: ldn-ui-v2
  namespace: learndevopsnow
spec:
  ingressClassName: nginx
  rules:
    - host: v2.learndevopsnow.it.com
      http:
        paths:
          - backend:
              service:
                name: ldn-ui-v2
                port:
                  number: 80
            path: /
            pathType: Prefix
  tls:
    - hosts:
        - v2.learndevopsnow.it.com
      secretName: ldn-tls-secret