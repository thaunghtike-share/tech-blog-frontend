apiVersion: apps/v1
kind: Deployment
metadata:
  name: ldn-ui
  namespace: learndevopsnow
  labels:
    app: ldn-ui
spec:
  replicas: 2
  selector:
    matchLabels:
      app: ldn-ui
  template:
    metadata:
      labels:
        app: ldn-ui
    spec:
      containers:
        - name: ldn-container
          image: mahar.azurecr.io/ldn-ui:latest
          ports:
            - containerPort: 3000
          imagePullPolicy: Always

---
apiVersion: v1
kind: Service
metadata:
  name: ldn-ui
  namespace: learndevopsnow
spec:
  type: ClusterIP
  selector:
    app: ldn-ui
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000